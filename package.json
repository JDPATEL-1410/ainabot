{
    "name": "whatsapp-saas-root",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "vercel-build": "cd frontend && npm install --legacy-peer-deps && npm run build"
    },
    "dependencies": {
        "mongodb": "^6.3.0",
        "express": "^4.18.2"
    }
}